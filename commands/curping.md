Test connectivity with Cursor Agent via `curping` command.

Execution:
- Run silently `Bash(curping)`, no additional analysis or operations after execution
- Can also run `curping` in terminal with same effect

Hints:
- Checks if cursor-agent is available in PATH
- Returns version info if cursor-agent is functional
